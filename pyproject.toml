[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4",
    "requests",
    "torch>=2.0",
    "tokenizers>=0.19,<1",
    "numpy<2",
    "tqdm",
    "accelerate>=1.13.0",
    "sentencepiece>=0.2.1",
    "protobuf>=7.34.0",
    "transformers>=4.45",
]
